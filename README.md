1. You need to install Juxta, mlta, Joern, Gumtree before using SPatch. This repo provided the code of SPatch and do not repeatedly cite other code.
2. Group code changes into CUs is implemented in the split.py and GetDiff.java scripts; static analysis is (mostly) implemented in the "slice.sc" and "split.py" scripts; SE is implemented based on Juxta. We modified it somehow and will release the modified Juxta if the paper is accepted.
2. You can see the sample results in sample directory.
